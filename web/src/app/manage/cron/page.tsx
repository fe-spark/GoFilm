"use client";

import React from "react";
import { Card, Typography } from "antd";

const { Title, Text } = Typography;

export default function CronPage() {
  return (
    <Card>
      <Title level={4}>定时任务管理</Title>
      <Text type="secondary">定时任务管理功能开发中...</Text>
    </Card>
  );
}
